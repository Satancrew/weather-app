import React from 'react'
import styles from './ThisDayInfo.module.scss';

type Props = {}

export const ThisDayInfo = (props: Props) => {
  return (
    <div className={styles.this__day_info}>ThisDayInfo</div>
  )
}